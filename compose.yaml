services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: invoicely
      POSTGRES_USER: invoicely
      POSTGRES_PASSWORD: invoicely
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
