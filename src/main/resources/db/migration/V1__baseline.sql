-- Baseline migration. Tables arrive in Task 2 (businesses, users, clients,
-- invoices, line_items, payments) — this file exists so Flyway has a versioned
-- starting point and the build proves the migration pipeline runs.
SELECT 1;
